#include <stdio.h>
#include <malloc.h>
#include <stdbool.h>

int found_max(int a[], int size);
int check_prost_or_not(int a[], int size);
bool is_prime(int n);
void print_num(int a[], int b[], int b_max);